set -e
set -o nounset
#set -o pipefail   # breaks "yes", I think

# already decompressed this one in jenkins
rm scripts.zip

for f in `/usr/bin/find | grep zip`
do
  yes | unzip $f
done
rm *.zip
rm -rf configuration*
lua scripts/deploy.lua
